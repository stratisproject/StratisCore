import { TestBed, inject } from '@angular/core/testing';

//import { ColdStakingService } from './cold-staking.service';

describe('ColdStakingService', () => {
//   beforeEach(() => {
//     TestBed.configureTestingModule({
//       providers: [ColdStakingService]
//     });
//   });

//   it('should be created', inject([ColdStakingService], (service: ColdStakingService) => {
//     expect(service).toBeTruthy();
//   }));
});
