import { AutoFocusDirective } from './auto-focus.directive';

describe('AutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFocusDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
